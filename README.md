# JournalCapture
A Flutter-based app that users capture journal entries using camera photos, applies OCR to extract text from images, and generates AI-powered summaries of each entry, all wrapped in a Material Design UI for easy daily logging and annotation.
